{name} {version} installer

This package was created using ScriptCenter on {date}.
It should be started either through ScriptCenter plugin (preferred) or as a standalone 
installer (by dropping it onto a 3dsmax viewport, or using the menu MaxScript -> Run Script).

Manually extracting files from this package and copying it to 3dsmax might result in the
script not being installed correctly.